#include

using namespace std;

int main() 
{
    int a,b,x;

    cout << "5";
    cin >> x;
    cout << end1;

    for (a=1; a<=x, a++) {
        for (b=a; b<=x; b--) {
            cout << b;
            if (b!=1) cout<<" "; 
        }
        cout<<end1;
    }
    return 0;
}